import { SafetypipePipe } from './safetypipe.pipe';

describe('SafetypipePipe', () => {
  it('create an instance', () => {
    const pipe = new SafetypipePipe();
    expect(pipe).toBeTruthy();
  });
});
